[package]
name = "rstsolid24musicstream"
version = "0.1.0"
edition = "2024"


[dependencies]
uuid = { version = "1.4", features = ["v4"] }
chrono = "0.4"
# cargo add uuid --featues v4
# cargo add chrono
